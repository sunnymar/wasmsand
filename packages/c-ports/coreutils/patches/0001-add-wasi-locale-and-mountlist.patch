--- a/gnulib/lib/getlocalename_l-unsafe.c	2026-04-26 16:30:05
+++ b/gnulib/lib/getlocalename_l-unsafe.c	2026-04-26 16:30:32
@@ -658,6 +658,9 @@
       };
       const char *name = ((struct __locale_t *) locale)->mb_cur_max == 4 ? "C.UTF-8" : "C";
       return (struct string_with_storage) { name, STORAGE_INDEFINITE };
+#elif defined __wasi__
+      /* Codepod (wasm32-wasip1): UTF-8 only, no per-locale state. */
+      return (struct string_with_storage) { "C.UTF-8", STORAGE_INDEFINITE };
 #else
  #error "Please port gnulib getlocalename_l-unsafe.c to your platform! Report this to bug-gnulib."
 #endif
--- a/gnulib/lib/mountlist.c	2026-04-26 16:30:05
+++ b/gnulib/lib/mountlist.c	2026-04-26 16:30:51
@@ -1244,7 +1244,12 @@
   }
 #endif
 
-#if MOUNTED_NOT_PORTED
+#if defined(__wasi__)
+  /* Codepod (wasm32-wasip1): no real libc-readable mount table.
+     Return empty list; /proc/mounts via the codepod VFS is the
+     authoritative answer for tools that actually want one. */
+  (void)need_fs_type;
+#elif MOUNTED_NOT_PORTED
 # error "Please port gnulib mountlist.c to your platform!"
 #endif
 
