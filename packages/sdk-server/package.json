{
  "name": "@codepod/sdk-server",
  "version": "0.0.1",
  "type": "module",
  "main": "dist/server.js",
  "bin": {
    "codepod-server": "dist/server.js"
  },
  "scripts": {
    "test": "bun test",
    "build": "tsup src/server.ts --format esm --dts"
  },
  "dependencies": {
    "@codepod/sandbox": "*"
  },
  "devDependencies": {
    "tsup": "^8.0",
    "typescript": "^5.7"
  }
}
